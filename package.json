{
  "name": "vlr-holdings",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs');fs.mkdirSync('dist',{recursive:true});fs.copyFileSync('index.html','dist/index.html');fs.readdirSync('public').forEach(f=>fs.copyFileSync('public/'+f,'dist/'+f));console.log('Built to dist/')\"",
    "preview": "npx serve dist"
  }
}
